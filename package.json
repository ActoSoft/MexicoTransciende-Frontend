{
  "name": "mexicotrasciende",
  "version": "0.1.1",
  "private": true,
  "dependencies": {
    "antd": "^3.23.1",
    "axios": "^0.19.0",
    "node-sass": "^4.12.0",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-router-dom": "^5.0.1",
    "react-scripts": "3.0.1",
    "react-toastify": "^5.3.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "lint": "eslint --ext .js \"src/**/*.{js,jsx}\"",
    "deploy": "./node_modules/.bin/firebase deploy",
    "firebase-use": "./node_modules/.bin/firebase use default",
    "firebase-apply": "./node_modules/.bin/firebase target:apply hosting mexicotrasciendeitesm mexicotrasciendeitesm"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run lint",
      "pre-push": "npm run lint"
    }
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "firebase-tools": "^7.2.2",
    "husky": "^3.0.3"
  }
}
